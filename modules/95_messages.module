#!modules/module.sh

help="
$(gettext "Read /var/log/messages.")
"

run () {
	ls -rt /var/log/messages* | while read -r path; do
		section "$path"
		if [ "${path: -3}" = '.gz' ] ; then
			zcat "$path"
		else
			cat "$path"
		fi
	done
}

# vim: ft=sh
