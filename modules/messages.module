#!modules/module.sh

help="
$(gettext "Read /var/log/messages.")
"

run () {
	ls /var/log/messages* | sort -r | while read path; do
		section "$path"
		if [ "${path: -3}" = '.gz' ] ; then
			zcat "$path"
		else
			cat "$path"
		fi
	done
}

# vim: ft=sh
